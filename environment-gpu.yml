name: polyellipsoid 
channels:
  - conda-forge
dependencies:
  - freud
  - gsd
  - hoomd=*=*gpu*
  - mbuild
  - numpy
  - pip
  - pytest
  - pytest-cov
  - python
  - pip:
    - git+https://github.com/cmelab/cmeutils.git@master
